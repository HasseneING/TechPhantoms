<form method="post" action="{{ path('niveau_delete', {'idN': niveau.idN}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ niveau.idN) }}">
    <button class="btn">Delete</button>
</form>
